import { pixels } from "./adptivePixels";
import { debounce } from "./debounce";
import { throttle } from "./throttle";
import { validateEmail, validateLogin, validatePassword } from "./validation";

export {
  validateEmail,
  validateLogin,
  validatePassword,
  throttle,
  debounce,
  pixels,
};
